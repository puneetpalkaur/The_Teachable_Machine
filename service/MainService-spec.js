describe('MainService', function() {

  beforeEach(module('TeachableMachine'));

  it('should ...', inject(function(MainService) {

    //expect(MainService.doSomething()).toEqual('something');

  }));

});
